#!/bin/bash

echo "---------------- SUMARIS {{ username }} :: services ------------------"
systemctl status sumaris-db-{{ username }} sumaris-pod-{{ username }} sumaris-db-backup-{{ username }}.service sumaris-db-backup-{{ username }}.timer